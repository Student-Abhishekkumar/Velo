[phases.setup]
nixPkgs = ["nodejs_20", "python311", "ffmpeg"]

[phases.install]
cmds = [
  "npm install"
]

[start]
cmd = "node server.js"
```

And add this to Railway's **Variables** tab:
```
YOUTUBE_DL_SKIP_PYTHON_CHECK=1